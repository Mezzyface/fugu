# Godot 4+ import & editor caches. Regenerated by Godot on import
# (`godot --headless --import`); never committed.
.godot/

# Misc generated artifacts
*.translation

# Mono / .NET (unused, kept for safety)
.mono/
data_*/

# Exported builds
/export/
/build/
